# Cluster ablation: V2.2 (mask applied, regression loss on ALL positions).
_base_: vivolm_llava_video.yaml
model:
  mask_variant: v2.2
train:
  output_dir: /data/vjuicefs_sz_ocr_wl/public_data/11193960/outputs/jepa_llava_video_v22
