# V4 对照臂: 纯 CE SFT, seed 0. 训练数据 = LLaVA-Video 原生 QA (temporal_qa_ratio=0:
# 不混入姊妹项目自建的帧序判别任务 —— V4 是流式验证, 裁判只有流式 benchmark).
_base_: r3_sft.yaml
train:
  seed: 0
  temporal_qa_ratio: 0.0
  output_dir: /data/vjuicefs_sz_ocr_wl/public_data/11193960/outputs/v4_ctrl_s0
